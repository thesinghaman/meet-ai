# General
.DS_Store
.AppleDouble
.LSOverride
.Trashes
.localized
.Spotlight-V100
.fseventsd

# Node.js & General Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
package-lock.json
# Uncomment the line above if you are using npm and want to ignore the lock file, though it's generally recommended to commit it.
yarn.lock
# Uncomment the line above if you are using Yarn and want to ignore the lock file, though it's generally recommended to commit it.
pnpm-lock.yaml
# Uncomment the line above if you are using pnpm and want to ignore the lock file, though it's generally recommended to commit it.

# Build output
dist
build
out
.next/
.vercel/
.output/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local
!.env.example

# Next.js
.next/
out/
next-env.d.ts

# TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo

# Drizzle ORM
drizzle/
*.sql

# Shadcn UI
components.json

# Tailwind CSS
tailwind.config.js.bak

# Inngest
.inngest/

# tRPC
.trpc/

# Tanstack Query
# No specific files to ignore, but caching might generate some if not handled in-memory.

# Polar
.polar/

# OpenAI API & Other sensitive keys
*.pem
*.key
credentials.json
# Add any other files that may contain sensitive API keys or credentials.

# Test runner caches
.jest-cache
.cache
.nyc_output
.eslintcache

# Editor directories and files
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Miscellaneous
coverage
.DS_Store?
._*
.Spotlight-V100
.Trashes
Icon?
ehthumbs.db
Thumbs.db

# Windows system files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
*.stackdump